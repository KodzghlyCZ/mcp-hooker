# Copy to .env and fill in. .env is gitignored.
# Caflou API token (Settings -> Account settings -> API -> Add token).
# Injected as "Authorization: Bearer <token>" on every upstream request.
CAFLOU_API_TOKEN=

# Optional: host port to publish (defaults to 8000).
# MCP_HOOKER_PORT=3003
